/* eslint-env mocha */

const expect = require("chai").expect;

describe("Suite", () => {

    it("should work", () => expect(true).to.be.true);

});

describe("Translator", () => {

    beforeEach((done) => {
        done();
    });

    it("should return missing key if key is not found", () => {

    });

    it("should return key if key is found", () => {

    });

    it("should replace %s", () => {

    });

});